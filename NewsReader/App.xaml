<?xml version="1.0" encoding="utf-8"?>
<Application xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:d="http://xamarin.com/schemas/2014/forms/design" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="NewsReader.App">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Brand Colors -->
            <Color x:Key="PrimaryBlack">#120203</Color>
            

           <Style TargetType="ContentPage" x:Key="PagePrimary" ApplyToDerivedTypes="True">
                <Setter Property="BackgroundColor" Value="{StaticResource PrimaryBlack}" />
            </Style>

            <Style TargetType="NavigationPage">
                <Setter Property="BarBackgroundColor" Value="{StaticResource PrimaryBlack}" />
                <Setter Property="BarTextColor" Value="White" />
            </Style>
            <Style TargetType="Label" x:Key="LabelPrimary">
                <Setter Property="TextColor" Value="White"></Setter>
            </Style>

            <Style TargetType="Label"
                   BasedOn="{StaticResource LabelPrimary}" />

            <Style TargetType="ListView">
                <Setter Property="BackgroundColor" Value="{StaticResource PrimaryBlack}"></Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>